tools:
- name: host-meson
  architecture: noarch
  source:
    subdir: ports
    version: "0.0.0"
  install:
  - args: [ "mkdir", "-p", "@PREFIX@" ]
  - args: [ "cp", "@SOURCE_ROOT@/support/meson-@OPTION:arch@.cross-file", "@PREFIX@/cross.txt" ]
